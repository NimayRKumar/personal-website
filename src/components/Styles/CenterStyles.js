import { createUseStyles } from 'react-jss'

const styling = {
    center: {
        textAlign: 'center',
        justifyContent: 'left'
    }
}

const useStyles = createUseStyles(styling)

export default useStyles